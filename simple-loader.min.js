/*** SIMPLE LOADER - A lightweight, mobile-first loading component* @version 1.0.0* @license MIT*/class SimpleLoader {constructor(options = {}) {this.config = {color: options.color || '#3498db',size: options.size || 'medium',type: options.type || 'spinner',overlay: options.overlay !== undefined ? options.overlay : true,overlayColor: options.overlayColor || 'rgba(0, 0, 0, 0.5)',speed: options.speed || 'normal',ariaLabel: options.ariaLabel || 'Loading...',className: options.className || '',customCSS: options.customCSS || {},container: options.container || document.body};this.element = null;this.overlay = null;this.isVisible = false;this.listeners = {};this._init();}_init() {this._createElements();this._applyStyles();}_createElements() {// Create overlayif (this.config.overlay) {this.overlay = document.createElement('div');this.overlay.className = 'simple-loader-overlay';this.overlay.style.cssText = `position: fixed;top: 0;left: 0;width: 100%;height: 100%;background: ${this.config.overlayColor};display: none;z-index: 9998;opacity: 0;transition: opacity 0.3s ease;`;}// Create loader containerthis.element = document.createElement('div');this.element.className = `simple-loader simple-loader-${this.config.type} ${this.config.className}`;this.element.setAttribute('role', 'status');this.element.setAttribute('aria-label', this.config.ariaLabel);this.element.style.cssText = `position: fixed;top: 50%;left: 50%;transform: translate(-50%, -50%);display: none;z-index: 9999;`;// Create loader content based on typethis._createLoaderContent();}_createLoaderContent() {const content = document.createElement('div');content.className = 'simple-loader-content';switch (this.config.type) {case 'spinner':content.innerHTML = '<div class="spinner"></div>';break;case 'dots':content.innerHTML = `<div class="dots"><div class="dot"></div><div class="dot"></div><div class="dot"></div></div>`;break;case 'pulse':content.innerHTML = '<div class="pulse"></div>';break;case 'bar':content.innerHTML = '<div class="bar"><div class="bar-inner"></div></div>';break;default:content.innerHTML = '<div class="spinner"></div>';}this.element.appendChild(content);}_applyStyles() {const sizeMap = {small: 32,medium: 48,large: 64};const speedMap = {slow: '1.5s',normal: '1s',fast: '0.6s'};const size = typeof this.config.size === 'number' ? this.config.size : sizeMap[this.config.size];const speed = speedMap[this.config.speed];this.element.style.setProperty('--loader-color', this.config.color);this.element.style.setProperty('--loader-size', `${size}px`);this.element.style.setProperty('--loader-speed', speed);// Apply custom CSSObject.keys(this.config.customCSS).forEach(key => {this.element.style[key] = this.config.customCSS[key];});}show() {if (this.isVisible) return;// Append to container if not already in DOMif (!this.element.parentNode) {if (this.overlay) {this.config.container.appendChild(this.overlay);}this.config.container.appendChild(this.element);}// Show with animationrequestAnimationFrame(() => {if (this.overlay) {this.overlay.style.display = 'block';requestAnimationFrame(() => {this.overlay.style.opacity = '1';});}this.element.style.display = 'block';this.isVisible = true;this._emit('show');});}hide() {if (!this.isVisible) return;if (this.overlay) {this.overlay.style.opacity = '0';setTimeout(() => {this.overlay.style.display = 'none';}, 300);}this.element.style.display = 'none';this.isVisible = false;this._emit('hide');}toggle() {if (this.isVisible) {this.hide();} else {this.show();}}update(options) {Object.assign(this.config, options);// Remove old elementif (this.element && this.element.parentNode) {this.element.parentNode.removeChild(this.element);}if (this.overlay && this.overlay.parentNode) {this.overlay.parentNode.removeChild(this.overlay);}// Recreatethis._init();// Show if was visibleif (this.isVisible) {this.isVisible = false;this.show();}this._emit('update', this.config);}setColor(color) {this.config.color = color;this.element.style.setProperty('--loader-color', color);}setSize(size) {this.config.size = size;const sizeMap = { small: 32, medium: 48, large: 64 };const sizeValue = typeof size === 'number' ? size : sizeMap[size];this.element.style.setProperty('--loader-size', `${sizeValue}px`);}setSpeed(speed) {this.config.speed = speed;const speedMap = { slow: '1.5s', normal: '1s', fast: '0.6s' };this.element.style.setProperty('--loader-speed', speedMap[speed]);}destroy() {this.hide();if (this.element && this.element.parentNode) {this.element.parentNode.removeChild(this.element);}if (this.overlay && this.overlay.parentNode) {this.overlay.parentNode.removeChild(this.overlay);}this.element = null;this.overlay = null;this.listeners = {};}on(event, callback) {if (!this.listeners[event]) {this.listeners[event] = [];}this.listeners[event].push(callback);}off(event, callback) {if (!this.listeners[event]) return;this.listeners[event] = this.listeners[event].filter(cb => cb !== callback);}_emit(event, data) {if (!this.listeners[event]) return;this.listeners[event].forEach(callback => callback(data));}}// Export for different module systemsif (typeof module !== 'undefined' && module.exports) {module.exports = SimpleLoader;}if (typeof window !== 'undefined') {window.SimpleLoader = SimpleLoader;}